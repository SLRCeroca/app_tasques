import 'package:flutter/material.dart';

class PantallaTasquesMediana extends StatelessWidget {
  const PantallaTasquesMediana({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Pantalla Mediana")));
  }
}
